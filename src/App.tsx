import React from 'react';
import Header from './components/Header';
import './App.scss';

export default function App(): JSX.Element {
    return (
        <main>
            <Header>a</Header>
        </main>
    );
};